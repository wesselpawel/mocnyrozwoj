/**
 * Blok intro — dieta na utrzymanie wagi
 */

export function getIntro(calorie: number): string {
  return `Dieta ${calorie} kcal jest przykładem jadłospisu, który pomaga utrzymać stabilną masę ciała przy zbilansowanym spożyciu.`;
}
