/**
 * Blok produkty — dieta na utrzymanie wagi
 */

export function getProducts(): string {
  return `Przy utrzymaniu wagi ważna jest różnorodność: pełnoziarniste zboża, warzywa, owoce, chude białko, zdrowe tłuszcze — bez ekstremalnych restrykcji.`;
}
