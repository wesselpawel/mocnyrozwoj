/**
 * Blok intro — dieta na redukcję
 */

export function getIntro(calorie: number): string {
  return `Dieta ${calorie} kcal pomaga stworzyć deficyt kaloryczny, który wspiera redukcję tkanki tłuszczowej przy zachowaniu masy mięśniowej.`;
}
